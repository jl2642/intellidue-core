# GitHub-2B Acceptance Pending

This branch contains the manifest, pointer and promotion-engine candidate. GitHub-2B closes only after pull-request CI passes and the accepted commit is merged to `main`.
