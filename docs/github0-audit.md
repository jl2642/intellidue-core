# GitHub-0 Intake Freeze

This public-core bootstrap was corrected before the first repository push.

Corrections:
- removed generated Python bytecode and cache directories;
- removed real-pilot names, counts and conclusions from public documentation;
- allowed the single synthetic ZIP fixture to be committed despite the general ZIP prohibition;
- aligned the product-manifest schema with the manifest builder;
- added deterministic manifest tests;
- added repository-hygiene enforcement;
- strengthened CI to install and exercise the CLI.

The license remains intentionally pending. Public visibility does not grant reuse rights without a selected license.
